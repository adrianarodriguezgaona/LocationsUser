<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="B4.PE3.RodriguezA.Pages.LocationUserPage">
   
        <ContentPage.ToolbarItems>
                <ToolbarItem Text="Save" Command="{Binding SaveCommand}"></ToolbarItem>
        </ContentPage.ToolbarItems>
        <ContentPage.Content>
                <ScrollView>
                    <StackLayout Style="{StaticResource MainLayout}">
                        <Label Text="Name" />
                        <Entry Text="{Binding Name}" Placeholder="Enter location name" Keyboard="Text" />
                        <Label Text="Theme Color" />
                        <Picker  Title="Select your color"  x:Name="pickerColor" ItemsSource="{Binding Colors}" SelectedItem="{Binding Color}" />
                        <Label Text= "Your theme color: {Binding Color}" />
                        
                        <Label Text="Visitdate" />
                        <DatePicker Date="{Binding DateVisit}" />
                       
                        
                    </StackLayout>
                </ScrollView>
        </ContentPage.Content>
       
    
</ContentPage>